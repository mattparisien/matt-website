import React, { useRef, useEffect } from "react";
import { StyledLogo } from "../styles/StyledTitle";


function Logo({ isContentHidden }) {
	const logoRef = useRef(null);

	// const [isSplit, chars, splitCount] = useSplit([logoRef.current], {
	// 	type: "chars",
	// 	charsClass: "char",
	// });

	return <>Matt</>;
}

export default Logo;
